export interface Community {
    id: string,
    name: string,
    description: string,
    banner_url: string,
    icon_url: string,
    created_by: string,
    is_private: boolean,
    is_restricted: boolean,
    created_at: string
}